<?xml version="1.0" encoding="utf-8"?>
<service>
  <short>Lumen Replication</short>
  <description>Live migration on the cluster's Core network: the hypervisor's peer connection on 16509 and the migration data stream on 49152 to 49215. Bind this service to Core interfaces only — a machine's memory never rides Management or an External network. The pooled storage link has its own service definition (lumen-pool).</description>
  <port protocol="tcp" port="16509"/>
  <port protocol="tcp" port="49152-49215"/>
</service>
