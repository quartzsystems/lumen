# Anaconda profile for Quartz Systems Lumen.
#
# NOT YET ACTIVE. This is a staged configuration for later installer
# branding work. To activate, package it (via lumen-logos or a dedicated
# lumen-anaconda-profile package) into /etc/anaconda/profile.d/lumen.conf
# inside the installer environment (requires a lorax rebuild of the boot
# media, not just mkksiso).

[Profile]
profile_id = lumen

[Profile Detection]
os_id = lumen
variant_id = lumen

[Product]
product_name = Quartz Systems Lumen

[User Interface]
# Artwork staged by lumen-logos under /usr/share/lumen-logos/anaconda/
custom_stylesheet =
hidden_spokes =
