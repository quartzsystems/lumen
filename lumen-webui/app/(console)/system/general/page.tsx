import { StubPage } from "@/components/StubPage";

/// Hostname, timezone, the appliance's own certificate, and the rest of the
/// settings that belong to the node rather than to anything running on it.
/// Nothing here yet: the two pages beside it are what this stage delivers.
export default function SystemGeneralPage() {
  return <StubPage title="General" />;
}
