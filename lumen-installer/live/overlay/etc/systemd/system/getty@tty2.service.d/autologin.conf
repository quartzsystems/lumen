# Debug escape hatch, live installer environment ONLY: root shell on tty2
# (Ctrl+Alt+F2). Never ships on an installed system.
[Service]
ExecStart=
ExecStart=-/sbin/agetty --autologin root --noclear %I $TERM
