# Build outputs
/build/
/dist/
*.iso
*.iso.sha256
*.rpm

# rpmbuild working trees
/rpmbuild/

# cargo (make targets build into build/cargo-target; this covers plain
# `cargo build` runs inside the app directory)
lumen-installer/app/target/

# Editor / OS noise
*.swp
*~
.DS_Store
Thumbs.db
